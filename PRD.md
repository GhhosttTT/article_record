# SOP 操作手册与视频生成插件 PRD

## 1. 产品概述

本产品是一款面向企业系统操作流程的 SOP 自动生成工具，由浏览器插件和配套 Web 应用组成。它用于记录用户在网页系统中的真实操作过程，将操作行为转化为结构化步骤数据，并基于同一份数据生成图文操作手册和讲解视频。

产品核心不是简单录屏，也不是通用视频剪辑工具，而是建立一套“操作节点”系统：每个关键操作都应具备可定位、可解释、可复现、可编辑和可渲染的能力。

核心链路：

```text
浏览器插件录制操作
-> 生成结构化操作节点
-> 自动解释每个节点
-> 截图并聚焦操作点
-> 生成 SOP 图文文章
-> 基于同一批步骤生成视频时间轴
```

## 2. 背景与问题

企业在培训、实施、交付、测试和客户支持中，经常需要制作系统操作手册。传统方式通常依赖人工截图、手写步骤、手动标注和录屏剪辑，存在以下问题：

- 制作成本高，重复劳动多。
- 步骤容易遗漏，截图与说明不一致。
- 操作重点不清晰，用户不知道该看哪里、点哪里。
- 图文手册和视频教程需要分别制作，内容容易不一致。
- 流程变化后，文档和视频都需要重新整理，维护成本高。

本产品希望通过浏览器插件自动采集操作节点，并基于同一份结构化数据生成文章和视频，降低 SOP 制作和维护成本。

## 3. 产品目标

### 3.1 核心目标

- 自动记录用户每一步关键网页操作。
- 为每个操作节点生成清晰、可编辑的说明。
- 自动截图并突出用户操作位置。
- 生成包含步骤、截图、重点放大或高亮的 SOP 文章。
- 基于文章步骤生成讲解视频，并在正确时间点展示对应步骤说明和焦点区域。
- 保证图文手册和视频内容来自同一份步骤数据，避免两套内容不一致。

### 3.2 非目标

MVP 阶段不追求完整替代专业视频剪辑工具，也不优先支持所有复杂网页场景，例如复杂 iframe、多层 Shadow DOM、Canvas 内部控件、远程桌面、非浏览器桌面软件、复杂权限协作、精细 PDF/Word 排版等。

## 4. MVP 版本边界

MVP 优先支持 Chrome 浏览器中的普通 Web 页面操作，重点验证以下闭环：

1. 记录点击、输入、选择、勾选、页面跳转等基础操作。
2. 为每个关键操作生成结构化节点。
3. 自动截图并记录目标元素位置。
4. 生成带操作点高亮的 HTML SOP 文章。
5. 支持用户编辑步骤标题、说明、顺序和高亮区域。
6. 记录多浏览器标签页切换，并在文章和视频中明确标注当前操作所在标签页。
7. 基于步骤截图生成基础 MP4 视频。

MVP 暂不承诺完整支持复杂 iframe、多层 Shadow DOM、Canvas 内部控件、真实录屏自动剪辑、鼠标轨迹重放、专业视频模板、复杂团队权限和精细导出版式。

## 5. 目标用户

- 企业实施顾问：制作客户系统操作手册。
- 客服与技术支持团队：沉淀常见问题处理流程。
- 培训团队：制作新员工培训 SOP。
- 测试与交付团队：记录标准业务流程和验收步骤。
- SaaS 产品团队：生成用户帮助中心文章和教程视频。

## 6. 核心概念

### 6.1 操作节点

操作节点是产品的最小数据单元。一次点击、输入、选择、勾选、上传、跳转或等待，都可以成为一个候选节点。

一个高质量节点需要具备：

- 可定位：知道用户操作了哪个元素。
- 可解释：知道这一步要表达什么。
- 可复现：后续用户能按说明重复操作。
- 可编辑：用户可以修改标题、说明、截图和高亮区域。
- 可渲染：可用于生成文章、图片标注和视频时间轴。

### 6.2 操作节点生成规则

系统应将用户行为转化为操作节点，但不是所有事件都应成为最终步骤。

应生成节点的事件：

- 点击按钮、链接、菜单项、表格操作入口。
- 在输入框中完成一次有效输入。
- 修改下拉框、单选框、复选框。
- 上传文件。
- 表单提交。
- 页面跳转、弹窗打开、弹窗关闭。
- 系统加载完成或状态变化明显的等待节点。

应合并的事件：

- 同一输入框连续输入应合并为一个输入节点。
- 同一表单中多个字段输入可以按配置合并为一个“填写表单”节点。
- 快速连续点击同一目标，默认只保留一次。
- 点击后立即发生页面跳转时，点击节点应关联跳转结果。

应过滤的事件：

- 无页面变化、无业务意义的空白区域点击。
- 鼠标移动、普通滚动、短暂停留。
- 用户误点后又立即撤销的操作。
- 密码输入的明文内容。

### 6.3 焦点渲染

焦点渲染是指根据操作节点中的元素坐标或点击位置，在截图中突出显示用户操作区域。

支持方式包括：

- 高亮框。
- 圆形聚焦。
- 局部放大镜。
- 自动裁剪重点区域。
- 半透明遮罩突出目标元素。

### 6.4 步骤时间轴

步骤时间轴用于把文章步骤映射到视频中的时间点。每个步骤需要对应视频片段、字幕、配音、焦点动画和展示时长。

## 7. 用户流程

### 7.1 录制流程

1. 用户安装浏览器插件。
2. 用户点击“开始录制”。
3. 用户在目标系统中正常完成业务流程。
4. 插件自动记录点击、输入、选择、勾选、页面跳转等事件。
5. 系统将事件转化为候选操作节点。
6. 系统根据合并和过滤规则生成最终步骤节点。
7. 插件自动截图，并记录操作元素坐标。
8. 用户点击“停止录制”。
9. 系统生成初版 SOP 文档。

### 7.2 编辑流程

1. 用户进入流程编辑器。
2. 查看自动生成的章节和步骤。
3. 修改步骤标题和说明。
4. 删除无意义步骤。
5. 合并或拆分步骤。
6. 调整步骤顺序。
7. 调整截图高亮区域。
8. 预览最终文章和视频效果。

### 7.3 文章生成流程

1. 系统读取操作节点列表。
2. 按页面、业务阶段或 AI 判断生成章节。
3. 每个步骤展示标题、说明、截图和焦点效果。
4. 生成可分享的网页文章。
5. MVP 优先支持 HTML，后续支持 Markdown、PDF、Word。

### 7.4 视频生成流程

1. 系统读取文章步骤和操作节点。
2. 为每个步骤生成视频片段时间段。
3. 在对应时间点展示步骤说明、字幕和高亮区域。
4. 可选生成语音讲解。
5. 通过 FFmpeg 或类似能力合成视频。
6. 用户预览并导出 MP4。

## 8. 功能需求

### 8.1 浏览器插件录制

插件应支持：

- 开始录制、暂停录制、继续录制、停止录制。
- 捕获点击事件。
- 捕获输入事件。
- 捕获复选框和单选框变化。
- 捕获下拉选择。
- 捕获页面跳转。
- 捕获表单提交。
- 记录当前页面 URL 和标题。
- 记录当前浏览器标签页、标签页切换、新标签页打开和切回原标签页等上下文。
- 记录目标元素文本、类型、属性和位置。
- 对敏感输入进行脱敏。
- 录制中断、页面刷新或网络异常时保留已采集数据。

### 8.2 节点识别

系统应从 DOM 中识别目标元素名称，优先级如下：

1. 可见文本。
2. aria-label。
3. label 关联文本。
4. placeholder。
5. title。
6. name 或 id。
7. 邻近文本。

系统应识别目标元素类型：

- 按钮。
- 链接。
- 输入框。
- 密码框。
- 复选框。
- 单选框。
- 下拉框。
- 菜单项。
- 上传控件。
- 表格行或单元格。

### 8.3 截图与焦点区域

系统应支持：

- 每个关键节点至少保存一张用于展示的截图。
- 记录元素 bounding box。
- 记录点击点坐标。
- 处理页面滚动偏移。
- 处理浏览器设备像素比。
- 生成带高亮框的步骤截图。
- 生成局部放大图。
- 允许用户手动调整高亮区域。

截图时机规则：

- 点击类操作：优先保存点击前截图，用于展示用户应该点击的位置；同时可选保存点击后截图，用于说明结果。
- 输入类操作：优先保存输入后截图，但敏感字段必须脱敏。
- 下拉选择类操作：如需展示选项列表，应在下拉展开时截图。
- 页面跳转类操作：保存跳转后页面截图。
- 弹窗类操作：保存弹窗出现后的截图。

焦点区域计算规则：

- 系统应记录目标元素的 bounding box、点击点、页面滚动偏移、设备像素比和截图尺寸。
- 渲染焦点图时，应将 DOM 坐标转换为截图坐标。
- 焦点区域默认在目标元素外扩 8-24px，避免高亮框紧贴控件边缘。
- 对于过小目标，应设置最小高亮区域。
- 对于超出视口或被遮挡目标，应提示用户手动调整。

### 8.4 步骤解释生成

系统应基于操作节点生成自然语言说明。

示例：

原始节点：

```json
{
  "action": "click",
  "targetText": "Create a Company",
  "targetType": "button"
}
```

基础说明：

```text
点击 Create a Company 按钮。
```

优化说明：

```text
在公司登录页面中点击 Create a Company，开始创建新公司。
```

系统应支持 AI 润色，但 AI 文案只作为草稿，用户必须可以手动编辑最终文案。默认说明应优先基于 DOM 信息、页面标题、URL 和前后状态变化生成，避免无依据扩写业务含义。

### 8.5 文章编辑器

文章编辑器应支持：

- 查看章节列表。
- 查看步骤列表。
- 编辑步骤标题。
- 编辑步骤说明。
- 删除步骤。
- 合并步骤。
- 拆分步骤。
- 调整步骤顺序。
- 编辑截图高亮区域。
- 手动打码截图敏感区域。
- 预览文章。
- 导出文档。

推荐编辑器布局：

- 左侧：章节和步骤列表。
- 中间：当前步骤截图预览和焦点区域编辑。
- 右侧：标题、说明、节点属性、隐私标记和导出配置。

### 8.6 视频生成

MVP 阶段采用截图合成视频，而不是剪辑真实录屏。

每个步骤生成一个视频片段，片段内容包括：

- 步骤截图。
- 操作点高亮框或放大镜。
- 底部字幕或侧边说明。
- 可选 TTS 配音。
- 简单转场动画。

视频生成应支持：

- 每个步骤绑定视频开始时间和结束时间。
- 在对应时间点展示字幕。
- 在对应时间点展示高亮框或放大镜。
- 每个步骤设置最短展示时长。
- 根据说明文字长度自动延长展示时长。
- 支持自动配音。
- 支持导出 MP4。

默认每个步骤展示 3-5 秒。后续版本再支持真实录屏片段、自动剪辑、鼠标轨迹和页面动画保留。

### 8.7 图文与视频一致性

文章步骤和视频片段必须来自同一份 ArticleStep 数据。

一致性原则：

- 系统不得维护两套独立文案。
- 视频字幕、配音文本默认复用文章步骤说明。
- 允许用户为视频单独覆盖 voiceoverText，但应明确标记为覆盖内容。
- 用户修改步骤标题、说明、顺序或高亮区域后，视频时间轴应同步更新。
- 当用户删除、合并或排序文章步骤时，视频时间轴应重新生成或提示用户确认更新。

### 8.8 多标签页切换标注

系统应支持用户在多个浏览器标签页之间完成同一个流程，并在文章和视频中清楚标注标签页变化。

需要支持的场景：

- 用户在 A 标签页点击链接，新开 B 标签页。
- 用户手动从 A 标签页切换到 B 标签页继续操作。
- 用户在 B 标签页完成操作后，切回 A 标签页。
- 用户在业务系统和邮箱、第三方认证、支付页面之间切换。
- 用户在同一站点多个标签页中处理不同对象。

节点要求：

- 新标签页打开应记录为 tab_open 或关联到触发点击节点。
- 标签页切换应记录为 tab_switch。
- 标签页关闭应记录为 tab_close，但默认只在影响流程理解时进入最终 SOP。
- 没有后续有效操作的快速标签页切换默认不进入最终 SOP。

文章展示要求：

- Tab 切换步骤使用“标签页切换”标识。
- 每个步骤应显示当前标签页名称或域名。
- 跨标签页流程应在章节或流程摘要中列出涉及的标签页。

视频展示要求：

- Tab 切换应生成 1-2 秒过渡片段。
- 过渡片段文案示例：`切换到标签页 B：邮箱收件箱`。
- 切换后的首个操作片段应显示当前标签页名称。

## 9. 隐私与安全要求

系统必须默认保护敏感信息。

默认脱敏字段：

- password、token、secret、验证码等字段内容不得保存明文。
- 邮箱、手机号、身份证号、银行卡号等应支持自动识别和脱敏。
- 输入值应区分 rawValue 和 maskedValue，默认只持久化 maskedValue。
- 用户可手动将截图区域打码。
- 导出文章和视频前，应提供敏感信息检查提醒。

数据存储要求：

- 截图、视频、步骤数据应绑定用户或团队权限。
- 临时录制数据应支持自动清理。
- 分享链接应支持关闭、过期时间和访问权限控制。
- 默认不公开分享内容，公开链接必须由用户主动开启。

## 10. 数据结构设计

### 10.1 录制会话

```json
{
  "id": "rec_001",
  "browser": "Chrome",
  "viewport": {
    "width": 1440,
    "height": 900,
    "devicePixelRatio": 1.25
  },
  "startedAt": "2026-07-22T10:00:00+08:00",
  "endedAt": "2026-07-22T10:05:00+08:00",
  "status": "completed"
}
```

### 10.2 操作节点

```json
{
  "id": "node_001",
  "recordingSessionId": "rec_001",
  "stepIndex": 1,
  "chapterId": "chapter_register",
  "action": "click",
  "timestamp": 5.2,
  "pageUrl": "https://example.com/company",
  "pageTitle": "ZKBio TimeCloud",
  "tab": {
    "tabId": 101,
    "tabAlias": "标签页 A：ZKBio TimeCloud",
    "windowId": 1,
    "domain": "example.com"
  },
  "target": {
    "type": "button",
    "text": "Create a Company",
    "selector": "...",
    "ariaLabel": null,
    "placeholder": null,
    "boundingBox": {
      "x": 941,
      "y": 488,
      "width": 174,
      "height": 38,
      "coordinateSpace": "viewport-css-pixel"
    }
  },
  "clickPoint": {
    "x": 1026,
    "y": 507,
    "coordinateSpace": "viewport-css-pixel"
  },
  "screenshot": {
    "imageId": "img_001",
    "path": "step_001_before.png",
    "width": 1440,
    "height": 900,
    "devicePixelRatio": 1.25
  },
  "privacy": {
    "containsSensitiveData": false,
    "maskedFields": []
  },
  "rawEvent": {},
  "generatedInstruction": "点击 Create a Company，开始创建新公司。",
  "businessMeaning": "进入公司创建流程。",
  "status": "auto_generated"
}
```

### 10.3 文章步骤

```json
{
  "id": "article_step_001",
  "nodeId": "node_001",
  "title": "点击 Create a Company",
  "description": "在公司登录页面中点击 Create a Company，开始创建新公司。",
  "image": "step_001_focus.png",
  "focusMode": "highlight",
  "focusBox": {
    "x": 933,
    "y": 480,
    "width": 190,
    "height": 54
  },
  "sortOrder": 1,
  "editStatus": "reviewed"
}
```

### 10.4 标签页切换节点

```json
{
  "id": "node_tab_001",
  "recordingSessionId": "rec_001",
  "action": "tab_switch",
  "timestamp": 42.5,
  "fromTab": {
    "tabId": 101,
    "tabAlias": "标签页 A：ZKBio TimeCloud 注册页",
    "url": "https://example.com/register"
  },
  "toTab": {
    "tabId": 102,
    "tabAlias": "标签页 B：邮箱收件箱",
    "url": "https://mail.example.com"
  },
  "reason": "user_switch",
  "generatedInstruction": "切换到标签页 B：邮箱收件箱，查收账号激活邮件。",
  "status": "auto_generated"
}
```

### 10.5 视频时间轴

```json
{
  "stepId": "article_step_001",
  "startTime": 0,
  "endTime": 4,
  "type": "operation",
  "visual": "step_001_focus.png",
  "caption": "点击 Create a Company，开始创建新公司。",
  "voiceover": "点击 Create a Company，开始创建新公司。",
  "voiceoverTextOverridden": false,
  "currentTabAlias": "标签页 A：ZKBio TimeCloud",
  "highlight": {
    "x": 933,
    "y": 480,
    "width": 190,
    "height": 54
  }
}
```

## 11. MVP 范围

### 11.1 必须实现

- Chrome 插件录制点击、输入、选择和勾选。
- 每个关键操作生成结构化节点。
- 每个节点保存截图。
- 记录元素坐标、点击点、视口尺寸和 devicePixelRatio。
- 自动生成步骤标题和说明。
- 在截图上绘制高亮框。
- 支持手动调整高亮区域。
- 生成 HTML 操作手册。
- 支持编辑步骤文案、删除步骤、合并步骤和调整顺序。
- 基于步骤截图生成基础 MP4 视频。
- 支持记录多标签页打开、切换和切回，并在 HTML SOP 和 MP4 中展示标签页切换提示。
- 默认脱敏密码和常见敏感字段。

### 11.2 可以延后

- Word 导出。
- PDF 精细排版。
- AI 自动分章节。
- 真实录屏剪辑。
- 鼠标轨迹重放。
- 高级视频模板。
- 自动配音。
- 企业权限。
- 多人协作。
- 流程版本管理。
- 帮助中心发布能力。

## 12. 推荐技术方案

### 12.1 插件端

- Chrome Extension Manifest V3。
- content script 监听用户操作。
- background service worker 管理录制状态。
- chrome.tabs.captureVisibleTab 进行截图。
- IndexedDB 临时存储录制数据。
- 使用消息通信将事件、截图和节点数据同步到 Web 应用或后端。

### 12.2 Web 端

- React 或 Vue。
- Canvas 渲染截图高亮、放大区域和打码区域。
- 结构化编辑器编辑文章步骤。
- HTML 作为第一优先级导出格式。

### 12.3 后端

- Node.js、Python 或 Java 均可。
- PostgreSQL 存储流程、节点、文章和视频任务。
- 对象存储保存截图和导出文件。
- FFmpeg 合成视频。
- 可选接入大模型生成步骤说明和章节。
- 可选接入 TTS 服务生成配音。

## 13. 关键难点

- 准确捕获每一步操作，避免漏记。
- 过滤无意义操作，避免生成流水账。
- 准确识别目标元素名称。
- 处理滚动、缩放、iframe、弹窗和下拉菜单。
- 敏感信息脱敏。
- 截图和坐标映射保持一致。
- AI 生成的说明可能不准确。
- 视频时间轴与文章步骤保持一致。
- 截图、视频、AI 和 TTS 会带来存储和计算成本。

## 14. 主要风险与降级策略

### 14.1 坐标偏移风险

风险：滚动、缩放、设备像素比、iframe 会导致高亮位置不准确。

降级：提供手动调整高亮区域；保存截图尺寸、视口尺寸和 devicePixelRatio；MVP 限制复杂 iframe 场景。

### 14.2 节点过多风险

风险：录制结果变成流水账，用户编辑成本高。

降级：提供自动过滤、连续输入合并、表单字段合并和用户手动删除能力。

### 14.3 AI 解释错误风险

风险：AI 可能误解业务含义。

降级：AI 文案只作为草稿，最终文案必须可编辑；默认说明优先基于 DOM 信息生成，避免无依据扩写。

### 14.4 敏感信息泄露风险

风险：截图、输入值、视频可能包含隐私数据。

降级：默认脱敏密码和常见 PII；导出前做敏感信息检查；支持截图手动打码。

### 14.5 视频生成成本风险

风险：真实录屏剪辑复杂度高，视频合成和 TTS 成本不可忽略。

降级：MVP 使用截图合成视频，后续再支持真实录屏；TTS 默认作为可选能力。

## 15. 成功指标

- 录制流程后，关键步骤识别准确率达到 90% 以上。
- 用户手动修改步骤数低于总步骤数的 30%。
- 每个步骤截图中的目标区域可被正确高亮。
- 一次录制可同时生成文章和视频。
- 生成的文章无需大量重写即可用于内部培训或客户指导。

## 16. MVP 验收标准

以 5 个典型 Web 流程进行测试，每个流程 10-30 步：

1. 点击、输入、选择、勾选、跳转节点可被正确捕获，关键步骤漏记率低于 10%。
2. 90% 以上节点能生成可理解的默认标题。
3. 90% 以上截图高亮区域能覆盖实际操作目标。
4. 用户可以在编辑器中完成删除、合并、排序、改文案、调高亮区域。
5. 可生成一篇完整 HTML SOP，并可正常分享或导出。
6. 可基于同一批步骤生成 MP4 视频，视频字幕顺序与文章步骤一致。
7. 多标签页流程中，文章和视频都能正确标注从哪个标签页切换到哪个标签页。
8. 密码字段不得出现在节点数据、截图展示文本或导出文件中。

## 17. 里程碑

### 阶段 1：原型验证

- 完成浏览器插件录制。
- 完成点击、输入、选择和勾选节点捕获。
- 完成截图和高亮框生成。
- 完成 HTML 文章生成。
- 完成基础多标签页切换记录和文章标注。

### 阶段 2：可编辑版本

- 增加步骤编辑器。
- 支持删除、合并、拆分、排序步骤。
- 支持手动调整高亮区域。
- 支持截图手动打码。
- 增加基础 AI 文案润色。

### 阶段 3：视频生成

- 基于步骤截图生成视频。
- 增加字幕和高亮动画。
- 支持可选 TTS 配音。
- 支持 MP4 导出。

### 阶段 4：商业化能力

- 用户账号。
- 团队空间。
- 分享权限。
- 导出 PDF 和 Word。
- 流程版本管理。
- 帮助中心发布能力。

## 18. 后续待补充

- 竞品分析：iGenFlow、Scribe、Tango、Guidde 等。
- 权限模型：个人空间、团队空间、公开链接、私密链接。
- 浏览器插件权限说明：需要哪些 Chrome 权限，以及为什么需要。
- 离线和失败恢复：录制中断、页面刷新、网络失败时如何保存。
- 国际化：是否需要中英文步骤说明。
- 成本估算：AI、TTS、视频合成、图片存储成本。
- 编辑器原型图和关键页面交互。

## 19. 一句话总结

本产品的核心是建立一套结构化的操作节点系统：录制用户行为，解释每个节点，生成带聚焦截图的 SOP 文章，并基于同一份节点数据生成讲解视频。
