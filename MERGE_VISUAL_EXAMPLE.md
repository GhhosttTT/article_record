# 打码区域合并效果对比

## 修改前 vs 修改后

### 场景：用户表单包含多个相邻的敏感字段

```
页面布局：
┌──────────────────────────────────┐
│ Username   [test@example.com   ] │  ← 邮箱字段
│ First Name [John              ] │
│ Last Name  [Doe               ] │
│ Password   [••••••••••••••••••] │  ← 密码字段
└──────────────────────────────────┘
```

---

## 修改前（简单去重）

### 扫描结果
自动扫描生成 **2 个独立的打码矩形**：

```
原始打码区域数组：
[
  { x: 100, y: 50,  width: 200, height: 30, source: "email_password_scan" },
  { x: 100, y: 110, width: 200, height: 30, source: "email_password_scan" }
]
```

### 视觉效果
```
┌──────────────────────────────────┐
│ Username   ████████████████████  │  ← 矩形1（独立）
│ First Name [John              ]  │
│ Last Name  [Doe               ]  │
│ Password   ████████████████████  │  ← 矩形2（独立）
└──────────────────────────────────┘
```

**问题**：
- ❌ 两个打码区域**视觉上分离**
- ❌ 看起来像是"拼接"的效果
- ❌ 中间的普通字段没有被覆盖

---

## 修改后（智能合并）

### 扫描结果
自动扫描生成 2 个独立打码矩形，然后**智能合并**：

```
原始打码区域数组：
[
  { x: 100, y: 50,  width: 200, height: 30 },  ← 邮箱
  { x: 100, y: 110, width: 200, height: 30 }   ← 密码
]

合并后打码区域数组：
[
  { x: 100, y: 50, width: 200, height: 90 }    ← 合并成一个大矩形
]
```

### 视觉效果
```
┌──────────────────────────────────┐
│ Username   ████████████████████  │  ┐
│ First Name ████████████████████  │  │ 合并后的
│ Last Name  ████████████████████  │  │ 连续区域
│ Password   ████████████████████  │  ┘
└──────────────────────────────────┘
```

**优点**：
- ✅ 打码区域**连续一体**
- ✅ 视觉效果更专业
- ✅ 减少了区域数量（从 2 个变为 1 个）

---

## 您的截图场景（用户列表表格）

### 场景描述
表格中有多行用户数据，Username 和 Email 列包含敏感信息。

### 修改前
```
┌────────────┬──────────────┬────────────┐
│ Username   │ First Name   │ Email      │
├────────────┼──────────────┼────────────┤
│ ████████   │ Normal       │ ██████████ │  ← 行1（2个独立矩形）
│ ████████   │ Normal       │ ██████████ │  ← 行2（2个独立矩形）
│ ████████   │ Normal       │ ██████████ │  ← 行3（2个独立矩形）
│ ████████   │ Superuser    │ ██████████ │  ← 行4（2个独立矩形）
│ ████████   │ Normal       │ ██████████ │  ← 行5（2个独立矩形）
└────────────┴──────────────┴────────────┘
```
总共 **10 个独立矩形**，每行看起来像"拼接"的效果。

### 修改后（期望效果）

#### 情况1：如果行距很小（≤8px）
```
┌────────────┬──────────────┬────────────┐
│ Username   │ First Name   │ Email      │
├────────────┼──────────────┼────────────┤
│ ████████   │ Normal       │ ██████████ │  ┐
│ ████████   │ Normal       │ ██████████ │  │
│ ████████   │ Normal       │ ██████████ │  │ Username 列
│ ████████   │ Superuser    │ ██████████ │  │ 合并为1个矩形
│ ████████   │ Normal       │ ██████████ │  ┘
└────────────┴──────────────┴────────────┘
                                  ┌───────┘
                                  └ Email 列合并为1个矩形
```
总共 **2 个大矩形**（Username 列 1 个 + Email 列 1 个）

#### 情况2：如果行距较大（>8px）
```
┌────────────┬──────────────┬────────────┐
│ Username   │ First Name   │ Email      │
├────────────┼──────────────┼────────────┤
│ ████████   │ Normal       │ ██████████ │  ← 行1（2个矩形，但每行内部连续）
│            │              │            │  ← 间隙
│ ████████   │ Normal       │ ██████████ │  ← 行2（2个矩形）
│            │              │            │
│ ████████   │ Superuser    │ ██████████ │  ← 行3（2个矩形）
└────────────┴──────────────┴────────────┘
```
总共 **6 个矩形**（每行 2 个），但每个矩形内部是连续的。

---

## 水平合并示例

### 修改前
```
[用户名] [邮箱@domain]  →  打码显示为：[████] [█████████]
                           ↑独立的  ↑独立的
```

### 修改后
```
[用户名] [邮箱@domain]  →  打码显示为：[██████████████]
                           ↑连续的一体化遮罩
```

---

## 垂直合并示例

### 修改前（表格列）
```
│ ████ │  ← 行1
│ ████ │  ← 行2  每个都是独立矩形
│ ████ │  ← 行3
│ ████ │  ← 行4
```

### 修改后（行距 ≤8px）
```
│ ████ │  ┐
│ ████ │  │ 合并为一个
│ ████ │  │ 连续的列
│ ████ │  ┘
```

---

## 技术参数

### 合并阈值说明
```javascript
const MERGE_THRESHOLD = 8; // 像素
```

**含义**：
- 两个矩形的**间隙 ≤ 8px** 时会合并
- 矩形的**Y坐标差 ≤ 8px** 且**高度差 ≤ 8px** → 视为同一行
- 矩形的**X坐标差 ≤ 8px** 且**宽度差 ≤ 8px** → 视为同一列

---

## 实际测试建议

### 测试用例1：单个表单
```html
<input type="text" name="email" value="user@test.com">
<input type="password" name="password" value="secret">
```
**期望**：如果两个输入框垂直距离 ≤ 8px，应合并为 1 个大矩形

### 测试用例2：表格列表（您的场景）
```html
<table>
  <tr><td class="username">user1</td><td class="email">user1@test.com</td></tr>
  <tr><td class="username">user2</td><td class="email">user2@test.com</td></tr>
  <tr><td class="username">user3</td><td class="email">user3@test.com</td></tr>
</table>
```
**期望**：
- 如果行距 ≤ 8px：Username 列合并为 1 个矩形，Email 列合并为 1 个矩形
- 如果行距 > 8px：每行保持独立，但每行内的水平字段会合并

---

## 调试技巧

如果合并效果不理想，可以：

### 1. 检查实际间隙
在浏览器控制台运行：
```javascript
document.querySelectorAll('input[type="password"]').forEach(el => {
  const box = el.getBoundingClientRect();
  console.log('Y:', box.y, 'Height:', box.height, 'Bottom:', box.y + box.height);
});
```

### 2. 调整合并阈值
如果表格行距较大（如 15px），可以增大阈值：
```javascript
const MERGE_THRESHOLD = 16; // 从 8 改为 16
```

修改位置：
- `extension/content.js` 第 1150 行左右
- `extension/shared/artifacts.js` 第 345 行左右

---

## 预期结果总结

| 场景 | 修改前 | 修改后 |
|-----|--------|--------|
| 相邻表单字段 | 多个独立矩形 | 1个连续矩形 |
| 表格列（行距≤8px） | 每行独立矩形 | 整列合并为1个矩形 |
| 表格列（行距>8px） | 每行独立矩形 | 每行独立，但行内连续 |
| 水平相邻字段 | 拼接效果 | 连续一体 |

✅ **核心改进**：从"视觉分离"变为"视觉连续"
